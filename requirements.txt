streamlit==1.56.0
anthropic==0.97.0
plotly==6.7.0
requests==2.33.1
python-dotenv==1.2.2
pandas==3.0.2
psycopg2-binary==2.9.12

# ─── 間接依存のピン止め (2026-07-13 追加) ─────────────────────────
# 2026-07-13 のデプロイ後、本番で exit 139 (SIGSEGV) のクラッシュループが発生。
# コードを旧版 (8d9b091) に戻しても「分析を実行」で再発したため、原因はコードではなく
# 再ビルドで混入した新しい間接依存 (C/Rust ネイティブ拡張) と特定。
# 最終正常ビルド (2026-05-12) 以前の最新版に固定して回避する。
# 更新する場合は1つずつ上げ、本番の「分析を実行」まで動作確認すること。
numpy==2.4.4
pyarrow==24.0.0
pillow==12.2.0
protobuf==7.34.1
tornado==6.5.5
watchdog==6.0.0
MarkupSafe==3.0.3
charset-normalizer==3.4.7
jiter==0.14.0
pydantic==2.13.4
pydantic-core==2.46.4
